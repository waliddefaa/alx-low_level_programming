#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'prints all the numbers of base 16 in lowercase'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;
	int z;


	for (i = 48; i <= 57; i++)
	{
		putchar(n);
	}

	for (z = 97; z <= 102; z++)
	{
		putchar(z);
	}
	putchar('\n');

	return (0);
}
