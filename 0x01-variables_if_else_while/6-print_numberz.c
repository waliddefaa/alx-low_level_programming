#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: 'this program print all single digit numbers'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int z;

	for (z = 48; z < 58; z++)
	{
		putchar(z);
	}
	putchar('\n');

	return (0);
}
